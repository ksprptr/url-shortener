/** Fixed id of the single `AuthState` row (the app has exactly one operator). */
export const AUTH_STATE_ID = 'singleton';

/** Subject every session is issued to — there is one operator and no user directory to name. */
export const OPERATOR_SUBJECT = 'operator';
