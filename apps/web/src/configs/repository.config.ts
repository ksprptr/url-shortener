/** Where the source lives — safe for the browser bundle, unlike the server-only app config. */
export const repositoryConfig = {
  slug: 'ksprptr/url-shortener',
  url: 'https://github.com/ksprptr/url-shortener',
};
