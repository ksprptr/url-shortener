# deps
**/node_modules
.pnpm-store

# builds
apps/api/dist
apps/web/.next
**/out
**/build
**/*.tsbuildinfo

# turbo
**/.turbo

# env — the images ship only the examples (the API validates its env against .env.example)
**/.env
**/.env.*
!**/.env.example

# vcs / tooling
.git
.github
.husky
.claude

# reference material (not part of the app)
example

# misc
**/coverage
**/*.md
**/.DS_Store
